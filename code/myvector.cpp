#include"myvector.h"



//�����⣬����
/*
template<typename T>
int MyVector<T>::binary_search(const T& v1)
{
	int left = 0, right = p - ps - 1;
	while (left < right)
	{
		int mid = left + (right - left) / 2;
		if (array[mid] < v1)
			left = right + 1;
		else
			right = mid;
	}
	if (array[left] = v1)
		return left;
	else
		return -1;
}
*/
